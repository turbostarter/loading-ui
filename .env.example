# Local: copy to `.env.local` (gitignored).
# Production: set the same VITE_ keys as Workers Builds → Settings → Build → Variables.
# Docs: https://developers.cloudflare.com/workers/ci-cd/builds/configuration/#environment-variables

VITE_POSTHOG_PROJECT_TOKEN=""
VITE_POSTHOG_HOST="https://eu.i.posthog.com"

POSTHOG_PERSONAL_API_KEY=""
POSTHOG_PROJECT_ID=""